"use client";

import Layout from "./layout";

const FilterPopupComp = ({ children }) => {
  return (
    <Layout>
      {/* <main className="flex-1 p-3 bg-white rounded-lg">{children}</main> */}
    </Layout>
  );
};

export default FilterPopupComp;
